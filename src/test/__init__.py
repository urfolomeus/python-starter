# Without this file pytest will not work, despite the application being a
# [namespace package](https://docs.python.org/3/reference/import.html#namespace-packages)
# and even though the application itself works as expected
